{% extends 'base1.html.twig' %}
{% block title%} Liste des Evènements{% endblock %}

{% block body %}

<div style="margin-top: 200px; margin-left: 5px">
<div class="titre" style="margin-left: 350px;margin-top:100px ">
<h1>Demandes des Evènements</h1>

<hr style="visibility: hidden ">
          <table border="2px"  style="margin-left:-280px; margin-right: 100px;text-align: center" class="table table-striped">

        <tr>

            {% if evenement %}
            <th>id</th>
            <th>nom</th>
            <th>date_evenement</th>
            <th>responsable</th>
            <th>description</th>
            <th>nbr places</th>
            <th>Categorie</th>
            <th>Action</th>
            <th>Action</th>




        </tr>
           <tr {%for e in evenement%}>
            <td>{{e.id_evenement}} </td>
            <td>{{e.nom_evenement}} </td>
            <td>{{e.date_evenement|date('Y-m-d')}} </td>
            <td>{{e.responsable}} </td>
            <td>{{e.description}} </td>
            <td>{{e.nbr_place}} </td>
            <td>{{e.categorie.Name}} </td>




            <td><a href="{{ path('accept',{'id_event':e.id_evenement}) }}" class="btn btn-dark" >Accepter</a></td>

            <td><a href="{{ path('refuse',{'id_event':e.id_evenement}) }}" class="btn btn-danger" onclick="return confirm('Etes vous sûr de refuser cet evènement?')";>Refuser</a></td>







        </tr {%endfor%}}>



    </table>

      <div style="margin-left: 450px">

</div>
    </div>
{% else %}
    <p>Aucun Evènement</p>
  {% endif %}

{% endblock %}